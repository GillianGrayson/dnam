# @package _global_

# to execute this experiment run:
# python run.py experiment=interpretation.yaml

defaults:
    - override /logger: null
    - override /model: null
    - override /datamodule: betas_pheno_datamodule.yaml
    - override /trainer: null
    - override /logger: null

checkpoint_path: "E:/YandexDisk/Work/pydnameth/datasets/meta/SchizophreniaDepressionParkinson/full/models/best/tabnetpl/2021-09-14_16-32-52/16/checkpoints"
checkpoint_name: "395.ckpt"
num_top_features: 10
num_examples: 10

datamodule:
    batch_size: 20
