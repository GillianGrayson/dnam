# @package _global_

# Global params
task: regression    # Task type. Options: ["regression", "classification"]
target: "Age"       # Target column name
target_label: "Age"
seed: 1337          # Random seed

# Inference params
path_ckpt: "D:/YandexDisk/Work/pydnameth/datasets/GPL21145/GSEUNN/special/044_small_immuno_clocks_revision/models/46/xgboost_trn_val_tst/multiruns/2023-04-25_21-09-57_1337/125/epoch_454_best_0001.model"
data_part_column: "best_${model.name}"
data_part_main: "trn"

# Data params
in_dim: null       # Number of input features
out_dim: 1      # Output dimension
embed_dim: 16   # Default embedding dimension

# Info params
print_config: False         # Print config?

# Directories and files params
project_name: ${model.name}_inference
base_dir: "D:/YandexDisk/Work/pydnameth/datasets/GPL21145/GSEUNN/special/044_small_immuno_clocks_revision"
data_dir: "${base_dir}"
work_dir: "${base_dir}/models/${project_name}"

# SHAP values params
is_shap: False                       # Calculate SHAP values?
is_shap_save: True                  # Save SHAP values?
shap_explainer: Tree            # Type of explainer. Options: ["Tree", "Kernel", "Deep", "Sampling"]
shap_bkgrd: trn                     # Type of background data. Options: ["trn", "all"]

# LIME weights
is_lime: False                      # Calculate LIME?
lime_save_weights: True             # Save LIME weights?
lime_bkgrd: trn                     # Type of background data. Options: ["trn", "all"]
lime_num_features: all              # Number of features. Options: [int, "all"]

# Plot params
num_top_features: 10  # Number of most important features to plot
num_examples: 200      # Number of samples or features examples to plot some SHAP figures
