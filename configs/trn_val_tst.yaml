# @package _global_

# specify here defaults
defaults:
  - _self_
  - experiment: covid19/classification/status         # Global parameters of experiment
  - datamodule: covid19/classification/status             # Information about dataset
  - trainer: gpu                          # Run configuration for DNN models
  - callbacks: default # none                    # Callbacks for DNN models
  - logger: none                          # Loggers for DNN models
  - hydra: default                        # Output paths for logs

  - model: covid19/classification/status/widedeep_tab_resnet                 # Model
  - hparams_search: covid19/classification/status/widedeep_tab_resnet        # Model-specific hyperparameters

  # Enable color logging
  - override hydra/hydra_logging: colorlog
  - override hydra/job_logging: colorlog
